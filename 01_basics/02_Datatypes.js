"use strict";   // treats entire code as newer version of javascript

// alert("Welcome");   // alert is not defined, we are using nodejs not browser 

let name= "Nick";   // string
let age= 18;    // number
let isAdult= true;  // boolean

console.table([name, age, isAdult]);

console.log(typeof(isAdult));   // typeof returns datatype


// number => integer values
// bigint => big integer values
// string => '' or ""(preferred)
// boolean => true / false
// null => standalone value
// undefined => value not defined
// symbol => unique

// object =>

console.log(typeof(null));  // object
console.log(typeof(undefined));    // undefined 